# DinoNuggets-Chatbot





## environment setup
* run```pip install -r requirements.txt``` from your IDE terminal to install all of the required dependencies at once
* you could also install them one by one if you prefer
